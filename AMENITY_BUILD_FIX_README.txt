Fixes the TypeScript build error introduced by the amenity-options overlay.

This restores the existing catalog group names and existing policy/property/calendar values,
then adds only the requested amenity options.

Extract over the repository root.
